import styled from 'styled-components';

const Goal = styled.h2`
  font-size: 1.5rem;
  color: #000;
  display: flex;
  flex-direction: column;
  margin-top: 5%;
  z-index: 100;

  span {
    font-size: 1.2rem;
    color: #000;
  }
`;

export default Goal;
